# modules are just .py scripts that you call in another .py script
# Packages are collection of modules

# __init__.py empty files are required inside the folder so that python can recognize it as a package

def sub_report():
    print("I am from sub package script")